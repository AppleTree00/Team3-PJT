# [기술 보강] APPLY4U MVP 개발 보고서

## 1. 기술 스택 (Tech Stack)
* **Frontend**: HTML5, Tailwind CSS (반응형 UI), JavaScript (ES6+)
* **Backend**: Python / Flask (轻량 프레임워크 기반 API 서버)
* **Database**: SQLite (빠른 프로토타이핑 및 이식성)
* **Deployment/Tool**: VS Code, Git/GitHub, Tailwind CDN

## 2. 핵심 아키텍처 및 작업 공정 (Workflow)
* **아키텍처 구성도**: 
    - [이곳에 아키텍처 흐름도나 다이어그램 삽입 예정]
* **3일 완성 작업 프로세스**:
    - 1일차: 사용자 중심 UI/UX 디자인 및 페이지 퍼블리싱
    - 2일차: Flask API 백엔드 연동 및 DB 스키마(a4u.db) 구축
    - 3일차: AI 기반 데이터 파싱 로직 및 QA 테스트 완료

## 2.1. PM 역할: 보고서 작성 및 문서 정합성 검증
* PM은 `pm.md`를 기준으로 `UI.md`, `dev.md`, `QA.md`를 교차 검증하여 보고서와 개발 문서 일관성을 확보합니다。
* 모든 코드 작성 및 수정 작업은 해당 파트의 MD 문서에 반영되어야 하며, PM에게 공유하여 기록 누락을 방지합니다.
* 모든 코드 변경은 충분한 주석과 설명을 포함해야 하며, 향후 고도화 및 기능 개선 시 코드 리뷰 비용을 줄이는 것이 목표입니다.
* 보고서는 기술 구현 결과뿐 아니라 진행 상황, 리스크 대응, 역할별 누락 체크를 함께 기록하여 발표 준비에 도움을 줍니다.
* 보고서 업데이트 항목:
    - 최신 QA 통과 현황
    - 병합 충돌 및 역할 분리 이슈 해결
    - 미구현 기능 및 시연 예외 안내 메시지

## 3. 주요 기능 및 AI 활용 전략
* **주요 기능**: 사용자 프로필 관리, 커리어 타임라인 생성, 이력서 작성/저장, AI 코칭, 관리자 대시보드, 문서 업로드/통계 조회.
* **AI 활용 (Co-pilot & Agent 활용)**:
    - **코드 생성**: AI 에이전트를 활용하여 HTML 템플릿 및 Flask 라우터 자동 생성.
    - **데이터 최적화**: 시니어 타겟 유저를 위한 문서 파싱 가이드 프롬프트 설계.
    - **디버깅**: 에이전트를 통한 실시간 에러 로그 분석 및 QA 피드백 반영.

## 4. 팀원별 기술 지원 요청 사항 (Call for Support)
- **기획자**: 기능 우선순위(MVP 범위) 재검토 및 최종 확정 요청.
- **UI/UX 디자이너**: 대시보드 및 프로필 페이지의 시니어 친화적 가독성 개선 가이드 요청.
- **프로그램 개발자**: 현재 `app.py`와 `a4u.db` 간 연동 최적화 및 보안 모듈 적용 요청.
- **QA**: 크로스 브라우저 테스트 및 사용자 시나리오 기반 에러 리포트 작성.

## 5. 주요 개발 이슈 및 해결 사례

### 5.1. 3일차 QA: 치명적 병합 충돌(Merge Conflict) 해결
*   **이슈 요약**: 3일차 QA 과정에서 사용자 대시보드(`dashboard.html`)와 관리자 페이지(`admin.html`)의 코드가 뒤섞이는 심각한 Git 병합 충돌이 발견되었습니다. 이로 인해 두 페이지 모두 정상 기능이 불가능하여 시연에 치명적인 결함으로 작용했습니다.
*   **AI 에이전트 활용**: QA 에이전트의 시나리오 기반 테스트 중 문제가 식별되었고, 개발 에이전트가 즉시 코드 분석에 착수했습니다. AI는 병합 과정에서 발생한 코드 중첩 및 누락 부분을 특정하고, 각 파일의 역할(사용자/관리자)에 맞는 코드 블록을 제안했습니다.
*   **해결 과정**:
    1.  **`dashboard.html`**: 관리자 전용 통계 UI 및 관련 JavaScript 로직을 제거하고, 순수 사용자 대시보드 기능으로 복원했습니다.
    2.  **`admin.html`**: 불필요한 로그인 로직과 사용자 페이지로의 리디렉션 코드를 제거하여 관리자 전용 페이지로 정상화했습니다.
*   **결과 및 교훈**: AI 에이전트를 활용한 신속한 QA 및 디버깅 프로세스를 통해 MVP 시연에 큰 위협이 될 수 있었던 문제를 조기에 해결했습니다. 이는 `pm.md`에 정의된 리스크 관리 및 QA 피드백 반영 절차가 효과적으로 작동했음을 보여주는 사례입니다. 또한, 향후 유지보수성 향상을 위해 각 페이지의 JavaScript를 별도 파일로 분리하는 작업의 필요성이 대두되었습니다.

### 5.2. 신규 기능 검증: 프로필 이미지 업로드 (QA PASS)
*   **이슈 요약**: 신규 구현된 '사용자 프로필 이미지 업로드' 기능이 MVP 기준 및 보안 정책에 맞게 동작하는지 QA를 진행했습니다.
*   **검증 결과 및 반영 사항**:
    1.  **보안/예외 처리**: 파일명 UUID 난수화, 허용 확장자 화이트리스트 검사, 업로드 디렉토리 부재 시 자동 생성 등 필수 안전장치 동작 확인 완료.
    2.  **UX 최적화**: 업로드 후 즉각적인 UI 반영을 가로막는 브라우저 캐싱 문제를 타임스탬프(`?t=timestamp`) 기법으로 우회한 점 확인 완료.
*   **결과**: MVP 목적에 완벽하게 부합하며 안정성이 보장된 훌륭한 코드로 판정되어 정식 시연 스펙으로 확정되었습니다.

### 5.3. 어드민 기능 고도화: 템플릿 파일 업로드 및 미리보기
*   **이슈 요약**: 기존 텍스트(HTML) 입력 방식의 템플릿 등록은 관리자 편의성이 낮고 실제 문서와의 괴리가 발생할 위험이 있었습니다.
*   **해결 방안**: PDF/WORD 파일을 직접 업로드하고 관리하는 시스템으로 전환했습니다.
*   **성과**:
    - **파일 기반 관리**: 실제 이력서 파일을 템플릿으로 등록하여 출력물 품질 보장.
    - **인쇄 및 미리보기**: 브라우저 내장 기능을 활용한 즉각적인 미리보기 및 인쇄 다이얼로그 호출 기능 구현.
    - **자동화**: 템플릿 삭제 시 서버 내 물리 파일도 동시 삭제되도록 로직 강화.

### 5.4. 보안 및 인프라 표준화
*   **환경 변수**: `.env.example` 도입으로 팀원 간 설정 공유를 표준화하고, `app.py`에서 자동으로 로드하도록 개선하여 배포 편의성을 높였습니다.
*   **보안**: `.gitignore` 최적화를 통해 개발 중 생성되는 임시 파일 및 DB 파일의 유출을 원천 차단했습니다.

---

## 6. 오늘 오후 작업 요약 (2026-06-24)

### 6.1 QA — 일반 계정 전체 플로우 검증 완료 (13개 시나리오 전건 PASS)
- 회원가입 → 로그인 → 이력서 CRUD → AI 코칭(Mock) → 제출처 관리 → 새 이력서 빈 폼 전 구간 검증
- 상세 결과: `QA.md` 섹션 4 참조

### 6.2 버그 수정 3건 완료
| ID | 심각도 | 수정 내용 |
|---|---|---|
| BUG-001 | HIGH | `GET /api/resumes` 샘플 혼재 → 사용자 이력서만 기본 반환, 샘플은 `?include_samples=true` 선택 |
| BUG-002 | MEDIUM | `applied_date` 컬럼 누락 → 모델·마이그레이션·라우트 3곳 동시 반영 |
| BUG-003 | LOW | 샘플 편집 차단 메시지 불명확 → `is_sample` 체크 추가, 명확한 한국어 403 반환 |

### 6.3 Replit 배포 환경 안정화
- `main.py` 진입점 정비 (sys.path + init_db 호출 구조)
- Nix 모듈 `python-3.11`로 고정 (3.12는 Free Tier 미지원)
- 워크플로우 커맨드 `python main.py`로 통일
- `replit.md` 프로젝트 문서 생성

### 6.4 현재 전체 시스템 상태
- **백엔드 API:** 전 엔드포인트 정상 동작 확인
- **AI 코칭:** OpenAI → Gemini → Mock 순 Fallback 구조 완비
- **DB:** 모든 마이그레이션 자동 적용 (init_db)
- **시연 준비:** 샘플 3종 + 전체 플로우 PASS — 최종 리허설 가능 상태

---

## 7. 최종 시연 체크리스트 (발표 직전 리허설 동선)

> 아래 순서대로 시연 5분 전에 점검합니다. 각 항목 완료 시 ✅ 체크하세요.

### 🔧 [사전 준비] 서버 및 환경 점검
- [ ] Replit 워크플로우 **"Start application"** 실행 중 확인 (초록 불)
- [ ] 브라우저에서 앱 프리뷰 열기 → 메인 페이지(`/`) 정상 로딩 확인
- [ ] AI 코칭 모드 확인: `GEMINI_API_KEY` 또는 `OPENAI_API_KEY` 미설정 시 Mock 모드로 진행 (정상 동작)
- [ ] 기존 테스트 데이터 잔존 시 demo 계정으로 초기화 후 시작

---

### 🎬 [시연 동선 1] 신규 사용자 가입 → 로그인

| 단계 | 화면 | 동작 | 기대 결과 |
|---|---|---|---|
| 1 | `login.html` | '회원가입' 탭 클릭 | 이름·이메일·비밀번호 입력 폼 표시 |
| 2 | `login.html` | 신규 계정 정보 입력 후 '가입' 클릭 | 가입 성공 → 로그인 화면으로 이동 |
| 3 | `login.html` | 가입한 계정으로 로그인 | `dashboard.html` 이동, 통계 카드 표시 |

---

### 🎬 [시연 동선 2] 이력서 신규 작성

| 단계 | 화면 | 동작 | 기대 결과 |
|---|---|---|---|
| 4 | `dashboard.html` | '새 이력서 작성' 버튼 클릭 | `builder.html?new=true` 이동, **빈 폼** 로딩 (기존 내용 없음) |
| 5 | `builder.html` | 인적사항 섹션 입력 (이름·이메일·전화번호) | 우측 미리보기 패널 실시간 반영 확인 |
| 6 | `builder.html` | 경력·보유기술·자기소개서 순서로 입력 | 상단 **진행 바(%)** 증가, **브레드크럼** 단계 이동 확인 |
| 7 | `builder.html` | '저장' 버튼 클릭 | `POST /api/resumes` 200 응답, 저장 완료 토스트 알림 |

---

### 🎬 [시연 동선 3] AI 코칭 요청

| 단계 | 화면 | 동작 | 기대 결과 |
|---|---|---|---|
| 8 | `builder.html` | 'AI 코칭 받기' 버튼 클릭 | 버튼 **비활성화** + "AI 코칭 분석 중..." **스피너** 표시 |
| 9 | `builder.html` | 응답 대기 (약 1~3초) | 스피너 해제 → 코칭 결과 텍스트 표시 (Mock 또는 실제 AI) |

---

### 🎬 [시연 동선 4] 제출처 등록 → 상태 관리

| 단계 | 화면 | 동작 | 기대 결과 |
|---|---|---|---|
| 10 | `dashboard.html` | '지원 현황' 또는 제출처 등록 버튼 클릭 | 제출처 입력 폼 표시 |
| 11 | 제출처 폼 | 회사명·포지션·지원일 입력 후 저장 | `POST /api/applications` 200, 목록에 신규 항목 추가 |
| 12 | 제출처 목록 | 상태 변경 (지원 예정 → 서류 통과 등) | `PUT /api/applications/{id}` 200, UI 상태 즉시 갱신 |

---

### 🎬 [시연 동선 5] 기존 이력서 수정

| 단계 | 화면 | 동작 | 기대 결과 |
|---|---|---|---|
| 13 | `resume.html` | 이력서 목록에서 본인 이력서 카드 선택 | **작성률(%)** 표시 카드 확인, 수정 버튼 클릭 |
| 14 | `builder.html` | 내용 일부 수정 후 '저장' | `PUT /api/resumes/{id}` 200, 수정 완료 토스트 알림 |

---

### 🚨 [예외 상황 대응 가이드]

| 상황 | 대응 방법 |
|---|---|
| AI 코칭 결과가 너무 짧거나 Mock 응답 | "현재 데모 환경에서는 Mock AI 피드백을 제공합니다. 실제 서비스에서는 OpenAI/Gemini 실시간 응답이 연동됩니다." |
| 미구현 버튼 클릭 시 토스트 팝업 표시 | "준비 중인 기능입니다. 데모 버전에서는 지원하지 않습니다." — 정상 동작으로 안내 |
| 샘플 이력서 편집 시도 시 오류 | "샘플 이력서는 편집할 수 없습니다" 안내 — 본인 이력서로 유도 |
| 서버 응답 지연 (3초 이상) | 로딩 스피너 정상 작동 중임을 안내, 새로고침 금지 |
| 페이지 새로고침 후 작성 내용 소실 우려 | localStorage 임시 저장 기능 시연 — 새로고침 후 내용 복원 확인 |

---

## 8. 어드민 계정 시연 동선 (admin@a4u.com / admin1234)

> 일반 사용자 시연 이후 "운영자 관점"을 보여주는 추가 시연입니다.  
> 총 3개 구역: **사용자·통계 관리** / **이력서 템플릿 관리** / **AI SQL 콘솔**

---

### 🔑 [사전] 어드민 로그인

| 단계 | 동작 | 기대 결과 |
|---|---|---|
| 1 | `login.html` → 이메일 `admin@a4u.com` / 비밀번호 `admin1234` 입력 후 로그인 | `admin.html` 어드민 대시보드 이동 |
| 2 | 상단 내비게이션 확인 | 사용자 수·이력서 수·제출처 수 통계 카드 표시 |

---

### 🎬 [어드민 동선 1] 사용자 및 서비스 통계 조회

| 단계 | 화면 | 동작 | 기대 결과 |
|---|---|---|---|
| 3 | `admin.html` | 대시보드 상단 통계 카드 확인 | `GET /api/admin/stats` — 전체 사용자 수, 이력서 수, 제출처 수 실시간 표시 |
| 4 | `admin.html` | '사용자 관리' 탭 클릭 | `GET /api/admin/users` — 가입된 사용자 목록 표 (이름·이메일·가입일) |
| 5 | `admin.html` | 특정 사용자 행 클릭 또는 상세 확인 | 해당 사용자 이력서 수·제출처 수 확인 |

---

### 🎬 [어드민 동선 2] 이력서 템플릿 관리

| 단계 | 화면 | 동작 | 기대 결과 |
|---|---|---|---|
| 6 | `admin.html` | '템플릿 관리' 탭 클릭 | 등록된 템플릿 카드 목록 표시 (카테고리·활성 상태 배지 포함) |
| 7 | `admin.html` | '+ 새 템플릿' 버튼 클릭 | 템플릿 등록 모달 오픈 (이름·카테고리·설명 입력 폼) |
| 8 | 모달 | PDF 또는 DOCX 파일 선택 (드래그&드롭 가능) | 파일명·크기 피드백 UI 표시, '저장' 클릭 → `POST /api/admin/templates` 성공 |
| 9 | 템플릿 카드 | '미리보기' 버튼 클릭 | `GET /api/admin/templates/{id}/file` — 새 창에서 파일 표시 |
| 10 | 템플릿 카드 | '인쇄' 버튼 클릭 | 브라우저 인쇄 다이얼로그 호출 (`window.print()`) |
| 11 | 템플릿 카드 | '활성화/비활성화' 토글 | 상태 즉시 전환, 비활성 템플릿은 사용자 화면에서 미노출 |

---

### 🎬 [어드민 동선 3] AI SQL 콘솔 (자연어 → SQL 자동 생성)

| 단계 | 화면 | 동작 | 기대 결과 |
|---|---|---|---|
| 12 | `admin.html` | 'AI 스키마 콘솔' 탭 클릭 | 자연어 입력 텍스트 영역 표시 |
| 13 | 콘솔 | "최근 7일 가입한 사용자 목록을 보여줘" 입력 후 실행 | Gemini AI가 SQLite 문법에 맞는 SQL 자동 생성 후 결과 테이블 표시 |
| 14 | 콘솔 | "이력서를 2개 이상 작성한 사용자를 찾아줘" 입력 | SQL 생성 → 결과 즉시 반환 |

> **💡 시연 포인트:** "운영자가 SQL을 몰라도 자연어로 데이터를 조회할 수 있다"는 점을 강조합니다.

---

### 🚨 [어드민 예외 상황 대응]

| 상황 | 대응 방법 |
|---|---|
| AI SQL 콘솔에서 응답 없음 | Gemini/OpenAI 키 미설정 시 Mock SQL 반환 — "AI 연동 시 실제 SQL이 자동 생성됩니다"로 안내 |
| 템플릿 파일 업로드 실패 | PDF/DOCX 외 파일 형식 → 400 에러 메시지 정상 노출 확인 |
| 일반 계정으로 `/admin.html` 직접 접근 | 세션 권한 검증 → 로그인 페이지로 리다이렉트 |
| 사용자 목록이 비어있음 | 시연 전 일반 계정 1개 이상 가입 완료 후 진행 권장 |

---

### 📌 전체 시연 타임라인 (권장 10분)

| 시간 | 구간 | 내용 |
|---|---|---|
| 0:00 ~ 1:00 | 사전 준비 | 서버 확인, 브라우저 열기 |
| 1:00 ~ 4:00 | 일반 사용자 동선 | 가입 → 이력서 작성 → AI 코칭 → 제출처 등록 |
| 4:00 ~ 5:30 | 기존 이력서 수정 | 목록 확인 → 작성률 → 수정/재저장 |
| 5:30 ~ 7:30 | 어드민 동선 | 통계 조회 → 템플릿 관리 → 미리보기 |
| 7:30 ~ 9:00 | AI SQL 콘솔 | 자연어 입력 → SQL 생성 → 결과 확인 |
| 9:00 ~ 10:00 | Q&A 대비 | 미구현 기능 팝업 시연, Mock AI 안내 멘트 |